"""Pipeline de Machine Learning (entrenamiento, evaluación y forecasting).

Este paquete aísla la lógica del entrenamiento de modelos del resto de la
aplicación. Se puede invocar como script (``python -m backend.pipeline.ml_pipeline``)
o lanzar desde ``backend/main`` vía ``subprocess``.
"""
